## 0.2.0 (2026-04-28)

### Notable features

- feat(cli): add global india-kit CLI — developer-facing CLI for scaffolding/validations, installs a global `india-kit` binary.

### Initial release highlights

- Package: `india-kit-cli`
- Installs `india-kit` executable (`bin` -> `dist/bin.js`) with shebang applied during build.
- Depends on `@india-kit/core` and bundles runtime dependencies in `dist`.

### Full release notes

#### Features

- add global india-kit CLI
